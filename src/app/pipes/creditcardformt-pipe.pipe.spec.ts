import { CreditcardformtPipePipe } from './creditcardformt-pipe.pipe';

describe('CreditcardformtPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcardformtPipePipe();
    expect(pipe).toBeTruthy();
  });
});
